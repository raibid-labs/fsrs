// Boolean Logic Example
// Demonstrates boolean operations and comparisons

let x = 10 in
let y = 20 in
let less = x < y in
let greater = x > y in
let equal = x = y in
let and_result = less && greater in
let or_result = greater || equal in
if and_result || or_result then 0 else 100
