[package]
name = "fusabi"
version = "0.1.0"
edition = "2021"
description = "A potent, functional scripting layer for Rust infrastructure."
repository = "https://github.com/fusabi-lang/fusabi"

[[bin]]
name = "fus"
path = "src/main.rs"

[lib]
name = "fusabi_demo"
path = "src/lib.rs"

[dependencies]
fusabi-frontend = { path = "../fusabi-frontend" }
fusabi-vm = { path = "../fusabi-vm", features = ["serde"] }
